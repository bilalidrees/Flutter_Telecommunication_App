class Prayer {
  String? namazName, namazTime, lightImage,darkImage;
  bool? isCurrentNamaz;

  Prayer(
      {this.namazName,
      this.namazTime,
      this.lightImage,
      this.darkImage,
      this.isCurrentNamaz});
}
